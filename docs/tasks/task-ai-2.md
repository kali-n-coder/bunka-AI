# タスク細分化：2. RAG & AIロジック (task-ai-2.md)

このドキュメントは `task-ai.md` の「2. RAG & AIロジック」を詳細に分解したものです。

現状では、`backend/app/services/ollama_client.py` に Ollama API へチャット送信するための基本クライアントとストリーミング受信用ジェネレータが実装されています。一方で、展示情報を検索するためのベクトルDB、Embedding、RAG用プロンプト生成、チャットAPIエンドポイント、フロントエンド連携は未実装です。

## 2.1 RAG用データソースの準備
- [ ] `docs/data/` ディレクトリを作成
- [ ] 展示情報・校内案内・注意事項を Markdown または JSON で管理する方針を決定
    - `docs/data_template.md` の「展示物リスト」と「AI用ナレッジベース」を入力元として利用
    - 展示ごとに1ファイルへ分割するか、単一JSONで管理するかを決める
- [ ] RAG投入用ドキュメントの最低限の項目を定義
    - 展示名
    - 概要
    - カテゴリ
    - 場所・座標
    - 所要時間
    - 見どころ
    - 注意事項
    - スタッフ向け補足の有無
- [ ] サンプル展示データを数件作成
- [ ] SQLite の `Exhibition` モデルと RAG用ナレッジの関係を整理
    - DBは一覧・待ち時間・位置情報に使用
    - RAG用ファイルは詳細説明・自然言語回答の根拠に使用

## 2.2 ベクトルDB (ChromaDB) の導入と初期化
- [ ] `backend/requirements.txt` に RAG 関連ライブラリを追加
    - ChromaDB
    - Embedding生成用ライブラリ
    - Markdown読み込み・分割に必要なライブラリ（必要な場合）
- [ ] `backend/app/services/vector_store.py` を作成
- [ ] ChromaDB の永続化ディレクトリを決定
    - 例: `backend/chroma_db/`
- [ ] コレクション名を設定で管理
    - 例: `HAKURYU_RAG_COLLECTION`
- [ ] 起動時または管理コマンドで ChromaDB を初期化する処理を実装
- [ ] 登録済みドキュメント件数を確認できる関数を実装
- [ ] ChromaDB 未初期化時・データ未投入時のエラーハンドリングを実装

## 2.3 ドキュメント投入・更新ロジック
- [ ] `backend/app/services/document_loader.py` を作成
- [ ] `docs/data/` 配下の Markdown / JSON を読み込む処理を実装
- [ ] ドキュメントを検索しやすい単位に分割するチャンク処理を実装
    - 展示単位
    - 見出し単位
    - 長文の場合は文字数またはトークン数単位
- [ ] 各チャンクにメタデータを付与
    - `source`
    - `exhibition_name`
    - `category`
    - `location`
    - `updated_at`
- [ ] 既存投入データの重複登録を防ぐ仕組みを実装
    - ファイルパス + ハッシュ値
    - または固定ID
- [ ] データ更新時に再投入できるスクリプトを作成
    - 例: `backend/scripts/ingest_documents.py`
- [ ] 投入結果をログに出力
    - 読み込みファイル数
    - チャンク数
    - スキップ数
    - エラー件数

## 2.4 Embedding生成方針の決定と実装
- [ ] Embeddingモデルを選定
    - Ollama の embedding API を使う
    - またはローカルで動く軽量な埋め込みモデルを使う
- [ ] `backend/app/services/embedding_client.py` を作成
- [ ] Embeddingモデル名を `backend/app/core/config.py` で設定可能にする
    - 例: `OLLAMA_EMBEDDING_MODEL`
- [ ] テキストからベクトルを生成する関数を実装
- [ ] Ollama未起動・モデル未取得時の例外処理を実装
- [ ] Embedding生成のタイムアウト・リトライ方針を決める
- [ ] 日本語テキストで検索精度が出るか確認する簡易検証を行う

## 2.5 セマンティック検索（類似度検索）の実装
- [ ] `backend/app/services/rag_service.py` を作成
- [ ] ユーザー質問をEmbedding化して ChromaDB から類似チャンクを取得する処理を実装
- [ ] 検索件数 `top_k` を設定可能にする
- [ ] 類似度スコアまたは距離をレスポンス内部で扱えるようにする
- [ ] カテゴリ・展示名などのメタデータフィルタ検索に対応
- [ ] 検索結果が少ない場合のフォールバックを実装
    - 通常チャットに切り替える
    - 「手元の資料では確認できない」と答えさせる
- [ ] 検索結果をプロンプトへ渡しやすい形式に整形する関数を実装
- [ ] 検索デバッグ用のAPIまたはスクリプトを用意
    - 例: 質問文、取得チャンク、スコア、参照元を表示

## 2.6 展示情報に基づいたプロンプトテンプレートの構築
- [ ] `backend/app/services/prompt_builder.py` を作成
- [ ] RAG検索結果を回答用コンテキストとして整形
- [ ] 展示情報・待ち時間・場所情報を統合してプロンプトに含める方針を決める
    - RAG文書: 詳細説明・見どころ・注意事項
    - SQLite: 展示名・カテゴリ・座標・現在の待ち時間
- [ ] ユーザー質問、検索結果、会話履歴をまとめた messages を生成する関数を実装
- [ ] コンテキストが長すぎる場合の切り詰めルールを実装
- [ ] 参照元を回答に含めるかどうかの方針を決定
- [ ] 回答できない質問へのテンプレートを用意
    - 資料にない内容を断定しない
    - 近い展示やスタッフ確認を案内する

## 2.7 白龍キャラクターのシステムプロンプト実装
- [ ] `backend/app/prompts/` ディレクトリを作成
- [ ] 白龍の基本人格・口調・役割を定義
    - 文化祭の来場者を案内する
    - 丁寧で親しみやすい
    - 展示情報に基づいて簡潔に答える
- [ ] 制約事項を定義
    - 根拠のない展示情報を作らない
    - 危険行為や立入禁止区域を案内しない
    - スタッフ専用情報を来場者に出さない
    - 待ち時間は最新データがある場合のみ断定する
- [ ] 来場者向け・スタッフ向けでプロンプトを分離するか検討
- [ ] システムプロンプトをファイル管理にするか、Python定数にするか決定
- [ ] プロンプト変更時に動作確認しやすいテスト質問セットを作成

## 2.8 ストリーミング形式での回答生成API
- [ ] `backend/app/api/` ディレクトリを作成
- [ ] チャット用エンドポイントを実装
    - 例: `POST /api/v1/chat`
    - 例: `POST /api/v1/chat/stream`
- [ ] リクエスト用 Pydantic スキーマを作成
    - `message`
    - `history`
    - `user_type`
    - `filters`
- [ ] レスポンス用 Pydantic スキーマを作成
    - `answer`
    - `sources`
    - `used_context`
- [ ] `StreamingResponse` または Server-Sent Events でストリーミングを返す
- [ ] 既存の `ollama_client.chat(..., stream=True)` を API から利用する
- [ ] RAG検索、プロンプト生成、Ollama送信を一連の流れとして統合
- [ ] Ollama未起動時のユーザー向けエラーを整える
- [ ] ストリーミング中断時のログ出力と例外処理を実装

## 2.9 フロントエンドとのAI連携
- [ ] `frontend/src/api/client.ts` にチャットAPI呼び出し関数を追加
- [ ] ストリーミングレスポンスを受け取る実装方針を決める
    - `fetch` + ReadableStream
    - Server-Sent Events
- [ ] チャット画面で必要になる状態を整理
    - 入力中メッセージ
    - 送信中状態
    - ストリーミング中の部分回答
    - エラー表示
    - 参照元表示
- [ ] API未起動・Ollama未起動・RAG未投入時の表示文言を用意
- [ ] モック回答でUIを先行開発できるモードを検討

## 2.10 RAG検証・品質確認
- [ ] `backend/tests/` または `backend/scripts/` に RAG検証スクリプトを作成
- [ ] サンプル質問セットを作成
    - 展示の概要を聞く
    - 場所を聞く
    - 注意事項を聞く
    - 存在しない展示を聞く
    - 雑談に近い質問をする
- [ ] 検索結果の妥当性を確認
    - 正しい展示チャンクが上位に来るか
    - 無関係なチャンクが混ざりすぎないか
- [ ] 回答の安全性を確認
    - 資料にない情報を作っていないか
    - 禁止事項を案内していないか
    - 待ち時間を古い情報として扱えているか
- [ ] ローカルLLMの応答速度を測定
    - 初回応答時間
    - ストリーミング速度
    - RAG検索時間
- [ ] 検証結果を `docs/reports/` に記録

## 2.11 設定・運用まわり
- [ ] `.env` に RAG / AI 関連設定を追加
    - `OLLAMA_BASE_URL`
    - `OLLAMA_MODEL`
    - `OLLAMA_EMBEDDING_MODEL`
    - `CHROMA_PERSIST_DIR`
    - `RAG_TOP_K`
- [ ] `run.py` 実行時に Ollama 接続確認を行うか検討
- [ ] 初回起動時に RAGデータ未投入であることを検知して案内する
- [ ] ChromaDB の生成物をGit管理対象にするか決める
- [ ] データ更新手順をREADMEまたはdocsに追記

